<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:viewmodels="clr-namespace:FitApp.ViewModels" 
             x:DataType="viewmodels:LoginViewModel"
             x:Class="FitApp.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout Spacing="3" Padding="15">
            <Label Text="FittApp V1.0" HorizontalOptions="Center" FontSize="Large" Margin="0,5"/>
            <Label Text="{Binding Error}" HorizontalOptions="Center" FontSize="Medium" Margin="0,5"/>
            <Label Text="Login:" FontSize="Medium" />
            <Entry Text="{Binding Login, Mode=TwoWay}" FontSize="Medium" />
            <Label Text="Password" FontSize="Medium" />
            <Entry Text="{Binding Password, Mode=TwoWay}" FontSize="Medium" IsPassword="True"/>
            <StackLayout Margin="0,20,0,0" Orientation="Horizontal">
                <Button Text="Login" IsEnabled="true" Command="{Binding LoginCommand}" HorizontalOptions="FillAndExpand"></Button>
                <Button Text="Register" Command="{Binding RegisterCommand}" HorizontalOptions="FillAndExpand"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>